import React from 'react';
import { FooterModel } from "../Models";

// Footer Component: This component is used to render the FooterModel, serving as the footer section of the application.
const Footer = () => {
  return (
    <>
      {/* Render the FooterModel component */}
      <FooterModel />
    </>
  );
};

export default Footer;
